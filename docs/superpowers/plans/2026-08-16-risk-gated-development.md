# 风险门禁开发流程实施计划

**目标：** 用已批准的风险门禁流程替换 `AGENTS.md` 中强制 TDD、全套 Superpowers、Kanban 和固定模型规则，同时保留项目领域约束与数据安全门禁。

1. 重写 `AGENTS.md` 的开发流程、测试、审查、Git 和前端协作规则。
2. 扫描冲突措辞，确认不再强制 TDD、每项任务写计划、Kanban 或固定代理模型。
3. 核对 Privnote、数据库、报价、品牌、图片和 Django 安全规则仍然存在。
4. 运行 `git diff --check`，提交中文 commit，并本地合并回默认主分支；不 push。
